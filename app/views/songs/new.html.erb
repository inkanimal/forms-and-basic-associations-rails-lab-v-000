<h1>Create a Song</h1>

<%= form_for @song  do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title %>
  <%= f.label :artist_id, :artist %>
  <%= f.text_field :artist_id %>
  <%= f.collection_select :genre, Genre.all, :name, :name %>
  <br />

  <%= f.submit %>
<% end %>


